<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Button Theme For Light Mode -->
    <style name="MatButtonLight" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">22sp</item>
        <!-- Black text color for light mode -->
        <item name="android:textColor">@color/black</item>
        <item name="backgroundTint">@android:color/transparent</item>
    </style>

    <!-- Button Theme For Light Mode -->
    <style name="MatButtonDark" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">22sp</item>
        <!-- white text color for light mode -->
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@android:color/transparent</item>
    </style>


</resources>